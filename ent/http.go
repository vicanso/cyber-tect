// Code generated by entc, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"github.com/facebook/ent/dialect/sql"
	"github.com/vicanso/cybertect/ent/http"
	"github.com/vicanso/cybertect/ent/schema"
)

// HTTP is the model entity for the HTTP schema.
type HTTP struct {
	config `json:"-" sql:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"createdAt,omitempty" sql:"created_at"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updatedAt,omitempty" sql:"updated_at"`
	// Status holds the value of the "status" field.
	Status schema.Status `json:"status,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Owner holds the value of the "owner" field.
	Owner string `json:"owner,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// Receivers holds the value of the "receivers" field.
	Receivers []string `json:"receivers,omitempty"`
	// Ips holds the value of the "ips" field.
	Ips []string `json:"ips,omitempty"`
	// URL holds the value of the "url" field.
	URL string `json:"url,omitempty"`
	// Timeout holds the value of the "timeout" field.
	Timeout string `json:"timeout,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*HTTP) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case http.FieldReceivers, http.FieldIps:
			values[i] = &[]byte{}
		case http.FieldID, http.FieldStatus:
			values[i] = &sql.NullInt64{}
		case http.FieldName, http.FieldOwner, http.FieldDescription, http.FieldURL, http.FieldTimeout:
			values[i] = &sql.NullString{}
		case http.FieldCreatedAt, http.FieldUpdatedAt:
			values[i] = &sql.NullTime{}
		default:
			return nil, fmt.Errorf("unexpected column %q for type HTTP", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the HTTP fields.
func (h *HTTP) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case http.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			h.ID = int(value.Int64)
		case http.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				h.CreatedAt = value.Time
			}
		case http.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				h.UpdatedAt = value.Time
			}
		case http.FieldStatus:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				h.Status = schema.Status(value.Int64)
			}
		case http.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				h.Name = value.String
			}
		case http.FieldOwner:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field owner", values[i])
			} else if value.Valid {
				h.Owner = value.String
			}
		case http.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				h.Description = value.String
			}
		case http.FieldReceivers:

			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field receivers", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &h.Receivers); err != nil {
					return fmt.Errorf("unmarshal field receivers: %v", err)
				}
			}
		case http.FieldIps:

			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field ips", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &h.Ips); err != nil {
					return fmt.Errorf("unmarshal field ips: %v", err)
				}
			}
		case http.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				h.URL = value.String
			}
		case http.FieldTimeout:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field timeout", values[i])
			} else if value.Valid {
				h.Timeout = value.String
			}
		}
	}
	return nil
}

// Update returns a builder for updating this HTTP.
// Note that you need to call HTTP.Unwrap() before calling this method if this HTTP
// was returned from a transaction, and the transaction was committed or rolled back.
func (h *HTTP) Update() *HTTPUpdateOne {
	return (&HTTPClient{config: h.config}).UpdateOne(h)
}

// Unwrap unwraps the HTTP entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (h *HTTP) Unwrap() *HTTP {
	tx, ok := h.config.driver.(*txDriver)
	if !ok {
		panic("ent: HTTP is not a transactional entity")
	}
	h.config.driver = tx.drv
	return h
}

// String implements the fmt.Stringer.
func (h *HTTP) String() string {
	var builder strings.Builder
	builder.WriteString("HTTP(")
	builder.WriteString(fmt.Sprintf("id=%v", h.ID))
	builder.WriteString(", created_at=")
	builder.WriteString(h.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", updated_at=")
	builder.WriteString(h.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", status=")
	builder.WriteString(fmt.Sprintf("%v", h.Status))
	builder.WriteString(", name=")
	builder.WriteString(h.Name)
	builder.WriteString(", owner=")
	builder.WriteString(h.Owner)
	builder.WriteString(", description=")
	builder.WriteString(h.Description)
	builder.WriteString(", receivers=")
	builder.WriteString(fmt.Sprintf("%v", h.Receivers))
	builder.WriteString(", ips=")
	builder.WriteString(fmt.Sprintf("%v", h.Ips))
	builder.WriteString(", url=")
	builder.WriteString(h.URL)
	builder.WriteString(", timeout=")
	builder.WriteString(h.Timeout)
	builder.WriteByte(')')
	return builder.String()
}

// HTTPs is a parsable slice of HTTP.
type HTTPs []*HTTP

func (h HTTPs) config(cfg config) {
	for _i := range h {
		h[_i].config = cfg
	}
}
